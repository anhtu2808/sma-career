# Backend API base URL (no trailing slash, no version prefix)
# Local: http://localhost:8080
# Production: https://api.smartrecruit.tech
NEXT_PUBLIC_API_URL=http://localhost:8080

# Root domain for subdomain-based routing
# The middleware extracts {slug} from {slug}.smartrecruit.tech
NEXT_PUBLIC_ROOT_DOMAIN=smartrecruit.tech
